<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".FoodSearchActivity">

    <EditText
        android:id="@+id/nutritionalSearchEditText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:hint="Type here"
        android:background="@color/primary"
        app:layout_constraintEnd_toEndOf="@+id/NutritionButton"
        app:layout_constraintStart_toStartOf="@+id/NutritionButton"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/NutritionButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Search"
        android:layout_marginTop="115dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/nutritionalSearchEditText" />

    <ScrollView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        app:layout_constraintTop_toBottomOf="@+id/NutritionButton">
    <EditText
        android:id="@+id/nutritionalResultsEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:enabled="false"
        app:layout_constraintTop_toBottomOf="@+id/NutritionButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp" />
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
